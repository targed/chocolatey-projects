# Cursor Package Parameters

This document describes the available package parameters for customizing the Cursor installation.

## Available Parameters

### `/System`
- **Description**: Installs Cursor system-wide instead of user-level
- **Effect**: Cursor will be installed for all users on the system
- **Use Case**: Recommended for multi-user shared machines or environments where application installations are typically global